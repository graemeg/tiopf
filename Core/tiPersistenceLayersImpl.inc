
  { These are here so that FPC can compile.  FPC doesn't support
  dynamic loading. }

  function TtiPersistenceLayers.LoadPackage(const Name: string): HMODULE;
  begin
    { do nothing }
  end;

  procedure TtiPersistenceLayers.UnloadPackage(Module: HMODULE);
  begin
    { do nothing }
  end;
  
  

  
  
